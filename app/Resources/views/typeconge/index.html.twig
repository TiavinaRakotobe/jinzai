{% extends 'AppBundle::layout.html.twig' %}
{% block content_body %}
    <section class="content">
        <div class="box box-default">
            <div class="box-header with-border">
                <h3 class="box-title">Type congé</h3>
                {% block box_tool %}
                    {{ parent() }}
                {% endblock %}
            </div>
            <div class="box-body">
            </div>
        </div>
        <div class="box box-primary">
            <div class="box-header">
                <h3 class="box-title">Résultats : {{ typeConges|length }} résultat(s) trouvé(s)</h3>
            </div>
            <a href="{{ path('typeconge_edit', {'id': null }) }}">
                <span class="btn">
                    <button type="button" name="new" class="btn btn-primary"><i class="fa fa-plus"></i>&nbsp;&nbsp;Ajouter nouveau</button></span>
            </a>
            <div class="box-body">
                <table id="liste-type-conge" class="table table-bordered table-condensed table-responsive table-striped table-hover blue-header">
                    <thead>
                        <tr>
                            <th>Code</th>
                            <th>Libelle</th>
                            <th>Statut</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for typeConge in typeConges %}
                            <tr>
                                <td><a href="{{ path('typeconge_show', { 'id': typeConge.id }) }}">{{ typeConge.code }}</a></td>
                                <td>{{ typeConge.libelle }}</td>
                                <td align="center">{% if typeConge.enabled %}<i class="fa fa-check-circle blue"></i>{% else %}<i class="fa fa-times-circle red"></i>{% endif %}</td>
                                <td>
                                    <a href="{{ path('typeconge_show', { 'id': typeConge.id }) }}">show</a>
                                    <a href="{{ path('typeconge_edit', { 'id': typeConge.id }) }}">edit</a>
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </section>
{% endblock %}
